@model test2.Models.DoctorModel.HealthRecordViewModel
@{
    Layout = "~/Views/Shared/Layout_Doctor.cshtml";
}

<div class="container mx-auto mt-10 pt-10 pb-10 px-8 lg:px-16 bg-white rounded-xl shadow-2xl max-w-4xl">
    <h2 class="text-4xl font-bold text-blue-600 mb-8">Chi tiết Hồ sơ y tế</h2>

    <!-- Phần thông tin bệnh nhân -->
    <div class="bg-blue-50 p-8 rounded-xl shadow-sm flex flex-col space-y-6">
        <!-- Hình ảnh và thông tin bệnh nhân -->
        <div class="flex items-center space-x-6">
            <img class="w-28 h-28 rounded-full border-4 border-blue-500 shadow-lg"
                 src= "https://img.lovepik.com/free-png/20210918/lovepik-patient-in-hospital-bed-png-image_400275909_wh1200.png" : Model.PatientImage)"
                 alt="Ảnh bệnh nhân" />
            <div>
                <h3 class="text-2xl font-semibold text-blue-600 mb-2">Thông tin bệnh nhân</h3>
                <p class="text-lg text-gray-800 flex items-center">
                    <i class="fas fa-user mr-2 text-blue-500"></i>
                    <strong>Tên:</strong> <span class="ml-24">@Model.PatientName</span>
                </p>
            </div>
        </div>

        <!-- Phần thông tin chẩn đoán và ghi chú -->
        <div class="bg-white rounded-lg shadow p-6">
            <h3 class="text-lg font-semibold text-blue-600 mb-2 flex items-center">
                <i class="fas fa-notes-medical mr-2"></i>
                Chẩn đoán
            </h3>
            <p class="text-lg text-gray-800">
                @Model.Diagnosis
            </p>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
            <h3 class="text-lg font-semibold text-blue-600 mb-2 flex items-center">
                <i class="fas fa-file-alt mr-2"></i>
                Mô tả
            </h3>
            <p class="text-lg text-gray-800">
                @Model.Description
            </p>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
            <h3 class="text-lg font-semibold text-blue-600 mb-2 flex items-center">
                <i class="fas fa-sticky-note mr-2"></i>
                Ghi chú
            </h3>
            <p class="text-lg text-gray-800">
                @(string.IsNullOrEmpty(Model.Note) ? "Không có ghi chú bổ sung." : Model.Note)
            </p>
        </div>

        <!-- Phần ngày khám -->
        <div class="flex justify-between bg-white rounded-lg shadow p-6">
            <div>
                <h3 class="text-lg font-semibold text-blue-600 mb-2 flex items-center">
                    <i class="fas fa-calendar-alt mr-2"></i>
                    Ngày khám
                </h3>
                <p class="text-lg text-gray-800">@Model.DateExam.ToString("MM/dd/yyyy")</p>
            </div>
        </div>
    </div>

    <!-- Nút Quay lại -->
    <div class="mt-10 flex justify-end space-x-4">
        <a asp-action="ViewAppointment"
           asp-route-id="@Model.AppointmentId"
           class="bg-blue-500 text-white px-6 py-3 rounded-lg hover:bg-blue-600 font-semibold shadow-md transition duration-300 ease-in-out">
            Quay lại cuộc hẹn
        </a>
    </div>
</div>
