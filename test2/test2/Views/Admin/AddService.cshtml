@{
    Layout = "~/Views/Shared/Layout_Admin.cshtml";  // Layout chính mà bạn đã định nghĩa
}
@model test2.Data.Specialty
@section Scripts {
    <script src="~/lib/jquery/jquery.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
}


@section Styles {
    <style>
        .layout-default_body_main {
            width: 100%;
        }

        form {
            margin: 20px;
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .first-text {
            font-size: xx-large;
            color: #4A5568;
            margin-bottom: 20px;
        }

        .section {
            margin-bottom: 40px;
        }

        .border {
            border-bottom: 1px solid #ccc;
            padding-bottom: 20px;
        }

        .grid-container {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 16px;
        }

        .grid-item {
            display: flex;
            flex-direction: column;
        }

        .grid-item-full {
            grid-column: span 2;
            display: flex;
            flex-direction: column;
        }

        label {
            font-weight: 500;
            margin-bottom: 8px;
        }

        input,
        textarea,
        select {
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            width: 100%;
            font-size: 14px;
            margin-bottom: 12px;
        }

        .flex-container {
            display: flex;
            align-items: center;
        }

        .photo-icon {
            width: 48px;
            height: 48px;
            background-color: #ccc;
            border-radius: 50%;
        }

        .change-btn {
            margin-left: 12px;
            padding: 8px 16px;
            border: 1px solid #ccc;
            background-color: #fff;
            cursor: pointer;
            border-radius: 4px;
            transition: background-color 0.2s;
        }

            .change-btn:hover {
                background-color: #f0f0f0;
            }

        .submit-container {
            display: flex;
            justify-content: flex-end;
            gap: 16px;
            margin-top: 20px;
        }

        button {
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
        }

        .cancel-btn {
            background-color: #f0f0f0;
            color: #333;
        }

        .save-btn {
            background-color: #0D6EFD;
            color: #fff;
        }

            .save-btn:hover {
                background-color: #357ABD;
            }

        .cancel-btn:hover {
            background-color: #e0e0e0;
        }

    </style>
}

    <main class="layout-default_body_main">
        <form method="post" asp-action="AddService" enctype="multipart/form-data">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

            <!-- Section 1: Service Information -->
            <div class="section">
                <div class="border">
                    <h1 class="first-text">Service Information</h1>

                    <div class="grid-container">
                    <div class="grid-item">
                        <label asp-for="SpecialtyId">Service ID</label>
                        <input asp-for="SpecialtyId" class="form-control" placeholder="Service ID">
                        @Html.ValidationMessageFor(model => model.SpecialtyId, "", new { @class = "text-danger" })
                    </div>

                    <div class="grid-item">
                        <label asp-for="SpecialtyName">Service Name</label>
                        <input asp-for="SpecialtyName" class="form-control" placeholder="Service Name">
                        @Html.ValidationMessageFor(model => model.SpecialtyName, "", new { @class = "text-danger" })
                    </div>

                        <!-- Photo -->
                        @* <div class="grid-item">
                            <label for="SpecialtyImg">Photo</label>
                            <div class="flex-container">
                                <input type="file" id="SpecialtyImg" name="SpecialtyImg" accept="image/*">
                            </div>
                        </div> *@

                        <!-- Short Description -->
                        <div class="grid-item">
                            <label for="ShortDescription">Short Description</label>
                            <input id="ShortDescription" name="ShortDescription" type="text" placeholder="Short Description">
                        </div>

                        <!-- Long Description -->
                        <div class="grid-item-full">
                            <label for="LongDescription">Long Description</label>
                            <textarea id="LongDescription" name="LongDescription" rows="3" placeholder="Detailed description of the service"></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Submit Button -->
            <div class="submit-container">
                <button type="button" class="cancel-btn">Cancel</button>
                <button type="submit" class="save-btn">Save</button>
            </div>
        </form>
    </main>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        const specialtyIdInput = document.getElementById('SpecialtyId');
        const specialtyNameInput = document.getElementById('SpecialtyName');

        specialtyIdInput.addEventListener('input', function () {
            const value = specialtyIdInput.value;
            const regex = /^[A-Za-z]\d*$/;

            if (!value) {
                showError('SpecialtyId', 'Specialty ID is required.');
            } else if (!regex.test(value)) {
                showError('SpecialtyId', 'Specialty ID must start with a letter followed by digits.');
            } else {
                clearError('SpecialtyId');
            }
        });

        specialtyNameInput.addEventListener('input', function () {
            const value = specialtyNameInput.value;
            if (!value) {
                showError('SpecialtyName', 'Specialty Name is required.');
            } else if (value.length > 30) {
                showError('SpecialtyName', 'Specialty Name cannot be longer than 30 characters.');
            } else if (!/^[A-Za-z\s]*$/.test(value)) {
                showError('SpecialtyName', 'Specialty Name must contain only letters.');
            } else {
                clearError('SpecialtyName');
            }
        });

        function showError(elementId, message) {
            const errorElement = document.querySelector(`#${elementId} + .text-danger`);
            if (errorElement) {
                errorElement.textContent = message;
            }
        }

        function clearError(elementId) {
            const errorElement = document.querySelector(`#${elementId} + .text-danger`);
            if (errorElement) {
                errorElement.textContent = '';
            }
        }
    });
</script>















@*
<main class="layout-default_body_main">
    <form>
        <!-- Section 1: Patient Information -->
        <div class="section">
            <div class="borsder">
                <h1 class="first-text">Patient Information</h1>

                <div class="grid-container">
                    <!-- ID -->
                    <div class="grid-item">
                        <label for="id">Service Name</label>
                        <input id="id" name="id" type="text" placeholder="Patient ID">
                    </div>

                    <!-- Photo -->
                    <div class="grid-item">
                        <label for="photo">Photo</label>
                        <div class="flex-container">
                            <div class="icon photo-icon"></div>
                            <button type="button" class="change-btn">Change</button>
                        </div>
                    </div>

                    <!-- First Name -->
                    <div class="grid-item">
                        <label for="first-name">Price</label>
                        <input id="first-name" name="first-name" type="text">
                    </div>

                    <!-- Last Name -->
                    <div class="grid-item">
                        <label for="last-name">Duration</label>
                        <input id="last-name" name="last-name" type="text">
                    </div>

                    <!-- Gender -->
                    <div class="grid-item">
                        <label for="gender">Status</label>
                        <select id="gender" name="gender">
                            <option>Male</option>
                            <option>Female</option>
                            <option>Other</option>
                        </select>
                    </div>

                    <!-- Date of Birth -->
                    <div class="grid-item">
                        <label for="dob">Category</label>
                        <input id="dob" name="dob" type="date">
                    </div>
                    <div class="grid-item-full">
                        <label for="experience">Description</label>
                        <textarea id="experience" name="experience" rows="3"></textarea>
                    </div>


                </div>
            </div>
        </div>


        <!-- Submit Button -->
        <div class="submit-container">
            <button type="button" class="cancel-btn">Cancel</button>
            <button type="submit" class="save-btn">Save</button>
        </div>
    </form>
</main> *@